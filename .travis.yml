language: generic

git:
  depth: 1

cache:
  directories:
    - $HOME/.stack/
    - $TRAVIS_BUILD_DIR/.stack-work/
    - $TRAVIS_BUILD_DIR/binaryen/

matrix:
  include:
    - os: linux
      dist: trusty
      sudo: false
      addons:
        apt:
          sources:
            - hvr-ghc
            - llvm-toolchain-trusty
            - ubuntu-toolchain-r-test
          packages:
            - clang-5.0
            - ghc-8.0.2
            - ninja-build

    - os: osx
      osx_image: xcode8.2

before_install:
  - export PATH=~/.local/bin:/opt/ghc/8.0.2/bin:$PATH
  - travis/before_install-`uname`.sh

install: true

script:
  - cd $TRAVIS_BUILD_DIR
  - stack --no-terminal --extra-include-dirs ~/.local/include --extra-lib-dirs ~/.local/lib build --haddock --no-haddock-deps --test --bench --no-run-benchmarks
